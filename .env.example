# 部署用的密碼設定範本。
# 在 NAS 的專案資料夾把這個檔複製成 .env，填入正式值。
# .env 不會進 git，git pull 也不會覆蓋它。
#
#   cp .env.example .env
#   然後編輯 .env
#
# JWT_SECRET 可用這個產生： openssl rand -hex 32

ADMIN_PASSWORD=請改成你的後台密碼
JWT_SECRET=請換成一長串隨機字元
